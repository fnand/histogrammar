language: java
# jdk: oraclejdk7

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
      - python3

cache:
  directories:
    - '$HOME/.m2/repository'

env:
  - CXX="g++-4.8"

script: ./test-all.sh
